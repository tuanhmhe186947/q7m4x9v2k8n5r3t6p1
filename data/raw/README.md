# Raw Data

Place local training images here:

```text
data/raw/images_clean/
```

Raw image data is ignored by Git to keep the repository small. For a public
release, publish these files through a dataset registry or data versioning tool
and add the download link to the main README.
