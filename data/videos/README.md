# Videos

Demo and research videos are local artifacts and are ignored by Git.

Default dashboard video path:

```text
data/videos/pigs101219_full.mp4
```

Use the checksum and URL metadata in `artifacts/manifest.yaml` to verify the
local copy before running tracking experiments.
