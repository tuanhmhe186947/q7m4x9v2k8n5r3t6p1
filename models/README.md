# Models

Model weights and exported artifacts are local runtime files and are ignored by
Git.

Expected local YOLO detector path:

```text
models/yolo/weights.pt
```

For a public release, publish trained weights through GitHub Releases, Zenodo,
Hugging Face, or another artifact registry, then document the exact URL and
checksum here.
