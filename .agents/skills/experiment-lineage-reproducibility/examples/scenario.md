# Scenario

Register a planned local smoke without training. Capture code and worktree state,
resolved configuration, five input hashes, software/hardware inventory, fold,
seed, and non-overwriting output paths before marking the run `planned`.
