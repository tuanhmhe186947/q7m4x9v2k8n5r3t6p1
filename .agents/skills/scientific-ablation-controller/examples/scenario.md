# Scenario

Accept `ResNet18 160 -> ResNet18 224` as a resolution comparison. Accept
`ResNet18 224 -> ResNet34 224` as a backbone comparison. Reject a candidate that
also changes temporal encoder, ROI, loss, and sampler without factorial design.
