# Scenario

Prepare an `actor_temporal` ResNet18 model factory entry, validate six-slot masks
and ten logits, run one synthetic forward pass, and write the checkpoint schema.
Do not download weights or start optimization during the skill dry-run.
