# Scenario

After one-fold PASS, profile one batch with AMP on a fixed config, search batch
size within a declared bound, test accumulation and resume, then estimate local
and rented-GPU cost. Do not change architecture or launch a full fold.
