# Scenario

Before adding a ResNet18 actor-temporal baseline, verify the frozen native-unit
manifest, grouped folds, explicit X whitelist, temporal-view lineage, and
fold-local fit policies. Return FAIL without starting training if any guard fails.
