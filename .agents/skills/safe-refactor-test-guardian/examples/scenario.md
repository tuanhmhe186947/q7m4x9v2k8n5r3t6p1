# Scenario

Before adding one actor-temporal factory mode, inspect its contracts and callers,
patch one module, compile changed files, run synthetic forward and schema checks,
show the diff, and provide rollback. Leave unrelated worktree changes untouched.
